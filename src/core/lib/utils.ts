/**
 * `cn` merges Tailwind class lists (clsx-style arguments, tailwind-merge conflict rules).
 * shadcn/ui v4 ships it as the `cn` package; this re-export is the alias `components.json` points at.
 */
export { cn } from "cn";
