import { PageLoading } from "@/core/ui/composites/loading-state";

/**
 * Settings is a list of section rows.
 */
export default function Loading() {
  return <PageLoading title="Settings" shape="list" />;
}
