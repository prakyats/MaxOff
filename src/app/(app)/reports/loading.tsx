import { PageLoading } from "@/core/ui/composites/loading-state";

/**
 * Reports (9.x) lead with the six KPI tiles, so the load traces tiles.
 */
export default function Loading() {
  return <PageLoading title="Reports" shape="tiles" />;
}
